## SECCIÓN 13 - Usu de la palabra Return y null
La palabra return debe regresar un tipo de dato por medio de un metodo: ej
```java

public class Ejemplo {

    publc static void main(String arg[]){

        // Declaro una variable (Object) de tipo persona
        Persona persona1 = new Persona("Pablo", "Veiga", true);

        // Llamo a un metodo que no regresa nada
        persona1.
    
    }

    class Persona {
        
        String nombre;
        String apellido;
        boolean trabaja;
        
        // constructor
        public Persona(String nombre, String apellido, boolean trabaja){
            this.nombre = nombre;
            this.apellido = apellido;
            this.trabaja= trabaja;
        
        }

        // Metodo que no regresa nada
        public void mostrarData() {
            System.out.printlne("Este metodo no regresa ninguna data")
        }

        public boolean personaTrabaja(Persona persona){
            return 
        }
    
    }

}

```
